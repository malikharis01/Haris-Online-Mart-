syntax = "proto3";

enum OperationType {
  CREATE = 0;
  DELETE = 2;
}

message Order {
  optional int32 id = 1;
  optional int32 product_id = 2;
  optional int32 quantity = 3;
  optional OperationType operation = 4;
}